import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export default function menubar() {
  return <View style={styles.contriner}></View>;
}

const styles = StyleSheet.create({
  contriner: {
    flexDirection: 'row',
  },
});
